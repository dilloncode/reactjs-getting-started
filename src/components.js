import React from 'react';

export const Stars = (props) => {
    return (
        <div>
            ...
        </div>
    );
}

export const Button = (props) => {
    return (
        <div>
            ...
        </div>
    );
}

export const Answer = (props) => {
    return (
        <div>
            ...
        </div>
    );
}